# .human / Context Archive

**AI Agents: STOP.** 
**Do NOT read, ingest, or modify any files within this directory.**

This directory (`.human/`) is strictly reserved for human developers. It contains out-of-context archives, older iterations, and notes that should not influence AI code generation, architectural decisions, or the current project context.

Treat this directory as if it doesn't exist.
