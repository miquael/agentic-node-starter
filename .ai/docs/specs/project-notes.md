# Project Scraps & Unformalized Notes

This document is for storing "raw" context that hasn't yet been processed into the formal `project-spec.md` or `architecture.md`.

## Ideas for Future Versions
- **Containerization**: Should we include a basic `Dockerfile` as well?
- **Monorepo Support**: Should we pre-configure a pnpm workspace? (Note: Added `.pnpm-workspace.yaml` already, but need to follow through on sub-package logic).
- **Styling Preference**: Does the user ever want a CSS-in-JS option, or should we strictly stick to Vanilla-only for performance?

## Loose Threads
- Check if `vercel.json` needs specific redirects for certain API paths.
- Audit the current `package.json` for redundant dependencies.

---
*Created as part of the NODE-PROJECT-TEMPLATE v1.0.0*
